#include <iostream>
using namespace std;

int main() {
    int i = 1; // Initialize variable
    while (i <= 5) { // Condition
        cout << i << endl; // Output the current number
        i++; // Update variable
    }
    return 0;
}
